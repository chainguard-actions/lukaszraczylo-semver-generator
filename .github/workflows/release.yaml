name: Test, build, release

on:
  workflow_dispatch:
  push:
    paths-ignore:
      - '**.md'
      - '**/release.yaml'
    branches:
      - main

permissions:
  id-token: write
  contents: write
  packages: write

jobs:
  release:
    uses: lukaszraczylo/shared-actions/.github/workflows/go-release.yaml@5cf1402555de4e9060aca71453d8259c0d40fe9c # main
    with:
      go-version: ">=1.25"
      docker-enabled: true
      rolling-release-tag: "v1"
      semver-config: "config-release.yaml"
    secrets: inherit

  update-action-version:
    needs: release
    runs-on: ubuntu-latest
    if: needs.release.outputs.version != ''
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          ref: main
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Update action.yml with release version
        env:
          VERSION: ${{ needs.release.outputs.version }}
        run: |
          SAFE_VERSION="$(printf '%s' "${VERSION}" | tr -cd 'a-zA-Z0-9.\-')"
          echo "Updating action.yml to version: ${SAFE_VERSION}"
          sed -i "s|ghcr.io/lukaszraczylo/semver-generator:[^\"]*|ghcr.io/lukaszraczylo/semver-generator:${SAFE_VERSION}|" action.yml
          echo "Updated action.yml:"
          grep "image:" action.yml

      - name: Commit and push
        env:
          RELEASE_VERSION: ${{ needs.release.outputs.version }}
        run: |
          SAFE_RELEASE_VERSION="$(printf '%s' "${RELEASE_VERSION}" | tr -cd 'a-zA-Z0-9.\-')"
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add action.yml
          if git diff --staged --quiet; then
            echo "No changes to commit"
          else
            git commit -m "chore: pin action.yml Docker image to v${SAFE_RELEASE_VERSION}"
            git push
          fi
